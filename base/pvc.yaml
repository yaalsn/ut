apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  name: pc-share
spec:
  storageClassName: sn-default
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
